---
import DocsLayout from "../../layouts/DocsLayout.astro";
---
<DocsLayout title="Tasks Window — Band Docs">
  <h1>Tasks Window</h1>
  <p>
    The Tasks window provides a centralized view of all coding agent tasks across your projects
    and workspaces. It opens as a dedicated native window from the Band desktop app, giving you
    a consolidated task management interface separate from the main dashboard.
  </p>

  <h2>Opening the Tasks Window</h2>
  <p>
    The Tasks window is accessed from the Band desktop app's menu. It opens as a separate 900x700
    pixel window. If the window is already open, focusing it again simply brings it to the front.
    On macOS, it uses an overlay title bar for a clean, native appearance.
  </p>

  <h2>Task List</h2>
  <p>
    The main area displays all tasks as cards, sorted by most recent. Each task card shows:
  </p>
  <ul>
    <li><strong>Prompt</strong> &mdash; The text prompt that was submitted to the coding agent, displayed as the primary content of the card.</li>
    <li><strong>Status badge</strong> &mdash; A colored badge indicating the current state: Running (with spinner), Completed (green), or Failed (red).</li>
    <li><strong>Project and branch</strong> &mdash; Shown as <code>project/branch</code> to identify which workspace the task belongs to.</li>
    <li><strong>Timing</strong> &mdash; When the task started (relative time like "5m ago") and its duration.</li>
    <li><strong>Session link</strong> &mdash; If the task has an associated session, a link takes you to the workspace detail view where you can see the full agent output.</li>
  </ul>

  <h2>Filtering Tasks</h2>
  <p>
    The toolbar below the header provides two filter dropdowns:
  </p>
  <ul>
    <li><strong>Project filter</strong> &mdash; Show tasks from a specific project only, or All Projects.</li>
    <li><strong>Status filter</strong> &mdash; Show only tasks with a specific status: All Statuses, Running, Completed, or Failed.</li>
  </ul>
  <p>
    A refresh button is available to manually reload the task list. The list also auto-refreshes
    every 5 seconds so it stays up to date.
  </p>

  <h2>Submitting a New Task</h2>
  <p>
    Click the <strong>New Task</strong> button to open the task submission dialog. The dialog has
    three fields:
  </p>
  <ol>
    <li><strong>Project</strong> &mdash; Select the target project from a dropdown of all registered projects.</li>
    <li><strong>Workspace</strong> &mdash; Select the target workspace (branch) within the chosen project.</li>
    <li><strong>Prompt</strong> &mdash; Describe what the agent should do. This is the instruction text sent to the coding agent.</li>
  </ol>
  <p>
    Once submitted, the task appears in the list with a Running status. You can also submit
    tasks via the CLI:
  </p>
  <pre><code>band tasks create &lt;workspace_id&gt; --prompt "Fix the failing tests"</code></pre>

  <h2>Canceling Tasks</h2>
  <p>
    Running tasks show a cancel button (X icon) on the right side of the task card. Clicking it
    sends a cancellation request to the agent. The task transitions to a failed state once the
    agent acknowledges the cancellation.
  </p>
  <pre><code>band tasks cancel &lt;task_id&gt;</code></pre>

  <h2>Re-running Tasks</h2>
  <p>
    Completed and failed tasks show a re-run button (rotate icon). Clicking it resubmits the
    same prompt to the same workspace, creating a fresh task execution.
  </p>
  <pre><code>band tasks rerun &lt;task_id&gt;</code></pre>

  <h2>Watching Task Output</h2>
  <p>
    While you cannot stream full agent output directly in the Tasks window, you can monitor a
    task's real-time output using the CLI:
  </p>
  <pre><code># Watch a specific task
band tasks watch &lt;task_id&gt;

# Watch the latest task for a workspace
band tasks watch --workspace &lt;workspace_id&gt;</code></pre>
  <p>
    The <code>band tasks watch</code> command streams task output to your terminal in real time,
    similar to <code>tail -f</code>.
  </p>

  <h2>Task Lifecycle</h2>
  <p>
    A task goes through the following states:
  </p>
  <ol>
    <li><strong>Running</strong> &mdash; The coding agent is actively working on the prompt. The task card shows a spinning loader.</li>
    <li><strong>Completed</strong> &mdash; The agent finished successfully. A green checkmark badge is shown.</li>
    <li><strong>Failed</strong> &mdash; The agent encountered an error or was cancelled. A red alert badge is shown.</li>
  </ol>
  <p>
    Completed and failed tasks remain in the list as history, allowing you to review what was
    done and re-run tasks if needed.
  </p>
</DocsLayout>
