---
import DocsLayout from "../../layouts/DocsLayout.astro";
---
<DocsLayout title="Cronjobs Window — Band Docs">
  <h1>Cronjobs Window</h1>
  <p>
    The Cronjobs window lets you create and manage scheduled coding agent tasks. Instead of
    manually submitting tasks, you can define recurring jobs that run on a cron schedule &mdash;
    perfect for routine maintenance, regular checks, or periodic code generation.
  </p>

  <h2>Opening the Cronjobs Window</h2>
  <p>
    The Cronjobs window is accessed from the Band desktop app's menu. Like the Tasks window,
    it opens as a separate 900x700 pixel native window. If already open, focusing it simply
    brings it to the front.
  </p>

  <h2>Cronjob List</h2>
  <p>
    The main area displays all cronjobs as cards. Each card shows:
  </p>
  <ul>
    <li><strong>Name</strong> &mdash; A descriptive name for the job (e.g., "Daily dependency check").</li>
    <li><strong>Scope badge</strong> &mdash; Either "project" or "workspace" indicating the scope of the job.</li>
    <li><strong>Prompt</strong> &mdash; The agent prompt that will be submitted when the job runs.</li>
    <li><strong>Cron expression</strong> &mdash; The schedule displayed in standard cron format (e.g., <code>0 */6 * * *</code>).</li>
    <li><strong>Target</strong> &mdash; The project or workspace this job runs against.</li>
    <li><strong>Last run info</strong> &mdash; When the job last ran and its result (Completed, Failed, or Skipped).</li>
    <li><strong>Enable/disable toggle</strong> &mdash; A switch to enable or disable the job without deleting it.</li>
  </ul>
  <p>
    Disabled jobs appear dimmed and will not execute on schedule, but they retain their
    configuration for when you re-enable them.
  </p>

  <h2>Creating a Cronjob</h2>
  <p>
    Click <strong>New Cronjob</strong> to open the creation dialog. Fill in the following fields:
  </p>
  <ul>
    <li><strong>Name</strong> &mdash; A human-readable name for the job.</li>
    <li><strong>Prompt</strong> &mdash; The instruction to send to the coding agent when the job triggers.</li>
    <li><strong>Cron expression</strong> &mdash; A standard five-field cron expression defining when the job should run.</li>
    <li><strong>Scope</strong> &mdash; Choose between project-scoped or workspace-scoped (see below).</li>
    <li><strong>Project</strong> &mdash; Select the target project.</li>
    <li><strong>Workspace</strong> (workspace scope only) &mdash; Select the specific workspace branch.</li>
    <li><strong>Enabled</strong> &mdash; Toggle whether the job starts in an enabled state.</li>
  </ul>

  <h3>Cron Expression Format</h3>
  <p>
    Cron expressions use the standard five-field format:
  </p>
  <pre><code>minute  hour  day-of-month  month  day-of-week
  0      */6       *           *        *       # Every 6 hours
  30      9        *           *       1-5      # 9:30 AM on weekdays
  0       0        1           *        *       # Midnight on the 1st of each month</code></pre>

  <h2>Project-Scoped vs Workspace-Scoped</h2>
  <p>
    Cronjobs can be scoped to two levels:
  </p>
  <ul>
    <li>
      <strong>Project scope</strong> &mdash; The job runs against the project's main branch.
      Useful for tasks like dependency updates, security scans, or code quality checks that
      should run on the primary codebase.
    </li>
    <li>
      <strong>Workspace scope</strong> &mdash; The job runs against a specific workspace branch.
      Useful for branch-specific tasks like running tests, checking for merge conflicts with
      main, or periodic progress reports on a feature branch.
    </li>
  </ul>

  <h2>Editing Cronjobs</h2>
  <p>
    Click the <strong>Edit</strong> button on any cronjob card to modify its name, prompt, cron
    expression, or enabled state. The scope and target cannot be changed after creation &mdash;
    delete the job and create a new one if you need to change those.
  </p>

  <h2>Manual Triggering</h2>
  <p>
    Every cronjob card has a <strong>Run Now</strong> button that triggers the job immediately,
    regardless of its schedule. This is useful for testing a new cronjob's prompt or running a
    job ahead of schedule. The manual run updates the "last run" timestamp and status.
  </p>

  <h2>Filtering</h2>
  <p>
    Use the project filter dropdown at the top of the window to show cronjobs for a specific
    project. The refresh button reloads the list manually, though it also auto-refreshes every
    5 seconds.
  </p>

  <h2>Deleting Cronjobs</h2>
  <p>
    Click the <strong>Delete</strong> button on a cronjob card to permanently remove it. When a
    workspace or project is removed from Band, its associated cronjobs are automatically cleaned up.
  </p>

  <h2>Run History</h2>
  <p>
    Each cronjob tracks its last execution with two fields:
  </p>
  <ul>
    <li><strong>Last run time</strong> &mdash; Displayed as a relative timestamp (e.g., "2h ago").</li>
    <li><strong>Last run status</strong> &mdash; One of Completed (green), Failed (red), or Skipped (yellow). A job is skipped if the target workspace already has a running task when the cron fires.</li>
  </ul>
</DocsLayout>
