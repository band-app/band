{
  "name": "@band-app/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "band": "bin/band.sh"
  },
  "scripts": {
    "build": "cargo build --release",
    "build:dev": "cargo build",
    "test": "pnpm --filter @band-app/server build && cargo test",
    "lint": "cargo clippy -- -D warnings",
    "format": "cargo fmt -- --check",
    "format:fix": "cargo fmt"
  }
}
