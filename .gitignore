node_modules/
dist/
band
# The bare `band` ignore above (the compiled CLI binary) also matches this
# source directory; force it back in. Its SKILL.md is the single source of
# truth, baked into the binary via include_str! — it must be committed.
!apps/cli/skills/band/
*.tgz
.DS_Store
.env
.env.*
__pycache__/
target/
*.vsix
*.tsbuildinfo
test-results/
playwright-report/
screenshots/
.last-run.json
apps/desktop/binaries/*
!apps/desktop/binaries/.gitkeep
